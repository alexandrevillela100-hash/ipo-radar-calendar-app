# Dependencies — never commit node_modules; npm install regenerates it.
node_modules/

# Build outputs
dist/
*.local

# Editor metadata
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment overrides — values live in Vercel's UI, not in the repo
.env
.env.local
.env.*.local

# TypeScript build info
*.tsbuildinfo
